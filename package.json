{
  "name": "recipe-box",
  "description": "React Reacipe box to perform CRUD and use local storage",
  "version": "0.0.3",
  "dependencies": {
    "react-dom": "16.0.0",
    "react": "16.0.0"
  },
  "devDependencies": {
    "react-scripts": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}